# 最終画像レビュー 2026-07-06

## 実行した工程

- 既存の第3巻画像を全削除し、画像生成を最初からやり直した。
- `figure_prompts.md` を作り直し、プロンプト書き出し工程と画像生成工程を分離した。
- SVG、仮図、同一テンプレート図は使わず、`$imagegen` の built-in 画像生成で全挿絵を作成した。
- 画像内ラベルは、英語ラベルを並べる方針をやめ、短い日本語ラベルを中心にした。`OS` は本文タイトルでも使う中心語として許容した。

## 生成・埋め込み

- 表紙画像: 1枚
- はじめに: 1枚
- 本文10ページ: 52枚
- 合計: 54枚

本文プレースホルダー53箇所は、`figure_prompts.md` の Caption とファイル名を正として Markdown 画像参照へ置換した。表紙は `cover-app-os-devices.jpg` としてビルド時に自動挿入される。

## final-quality-checklist 観点

### 商品価値

画像は、OS、インストール、メモリ、プロセス、ファイル、GUI、イベント、ドライバ、権限、アップデートを、読者が見たことのある操作から見える構図へ落とした。単なる箱矢印ではなく、棚、作業机、窓口、鍵、区切り、修理、地図などの比喩を使い分けている。

### 内容の深さ

読者が存在を知っているOS、許可、更新は、管理、許可、入れ替えの必要性が見える絵にした。読者が存在を知らない可能性があるプロセス、イベント、ドライバ、サンドボックスは、それがあると何をまとめて扱えるかが見える絵にした。

### 例と比喩

比喩は本文の技術本体へ戻れるものに限定した。たとえば、保存場所とメモリは棚と作業机、ドライバは共通の依頼を機械ごとの命令へ変える翻訳窓口、権限は鍵として描いた。

### 情報密度

図1点につき、本文の理解を少なくとも1つ補うようにした。特にページ8のドライバ図は、共通の依頼、機械ごとの命令、結果、エラーの往復まで示しており、前回の uplift 不足を補っている。

## 採用時の注意

- 一部の図には、`IMG_001.jpg` や座標値のような小さな技術的文字列が入っている。英語ラベルで説明を済ませる状態ではなく、ファイル名や座標という対象そのものを示す表現として採用した。
- 日本語ラベルは概ね読める。細かな字間のばらつきはあるが、本文キャプションと合わせれば理解を妨げない範囲と判断した。
- 実在企業ロゴや特定OSのUIに見えるものは採用していない。

## 生成後チェック

- `tools/build_volume.py --check` 成功。
- `tools/check_volume_quality.py --require-pdf` 成功。
- 挿絵プレースホルダー残存なし。
- `git diff --check` 成功。
